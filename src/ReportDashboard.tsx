import React from 'react';

const ReportDashboard: React.FC = () => {
  return (
    <div>
      <h2>Reporting Dashboard</h2>
      <p>Analytics and charts (e.g., using recharts - add dependency if needed).</p>
    </div>
  );
};

export default ReportDashboard;